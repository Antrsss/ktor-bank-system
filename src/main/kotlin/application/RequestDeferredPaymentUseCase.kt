package com.example.usecases

import com.example.domain.LoanPeriod
import com.example.domain.entities.loan_obligations.DeferredPayment
import java.util.*

/*
class RequestDeferredPaymentUseCase(
    private val deferredPaymentRepository: DeferredPaymentRepository
) {
    fun execute(clientId: UUID, amount: Double, period: LoanPeriod): DeferredPayment {
        val deferredPayment = DeferredPayment(clientId, amount, period)
        deferredPaymentRepository.createDeferredPayment(deferredPayment)
        return deferredPayment
    }
}*/
